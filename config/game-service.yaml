# Game Service Configuration (gRPC)

service:
  name: "game-service"
  host: "0.0.0.0"
  port: 9090
  mode: "release"

grpc:
  host: "0.0.0.0"
  port: 9090
  reflection: true

# Import shared configurations
shared:
  database: "shared.yaml"
  redis: "shared.yaml"
  logging: "shared.yaml"
  jwt: "shared.yaml"
  tracing: "shared.yaml"
