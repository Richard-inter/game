# RPC WhackAMole Service Configuration (gRPC)

service:
  name: "rpc-whackamole-service"
  host: "0.0.0.0"
  port: 9097
  mode: "release"

grpc:
  host: "0.0.0.0"
  port: 9097
  reflection: true

# Database Configuration
whackamole_database:
  host: "localhost"
  port: 3309
  user: "gameuser"
  password: "gamepass"
  name: "game"
  charset: "utf8mb4"

# Import shared configurations
shared:
  redis: "shared.yaml"
  logging: "shared.yaml"
  jwt: "shared.yaml"
  tracing: "shared.yaml"
